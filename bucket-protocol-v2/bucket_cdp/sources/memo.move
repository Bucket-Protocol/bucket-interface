module bucket_v2_cdp::memo {
    use std::string::String;

    public fun manage(): String { abort 0; }

    public fun donate(): String { abort 0; }

    public fun liquidate(): String { abort 0; }

    public fun interest(): String { abort 0; }
}
